{
    "name": "your-project-name",
    "version": 2,
    "builds": [
      { "src": "*.js", "use": "@vercel/static" }
    ],
    "routes": [
      { "src": "/api/(.*)", "dest": "/api/" },
      { "src": "/(.*)", "dest": "/" }
    ]
  }
  